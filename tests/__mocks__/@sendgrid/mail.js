// That's good that in test environment, no ApiKeys should be set
// and no emails should be sent. ;)
// But we could write something if we really want for tests.
module.exports = {
    setApiKey() {

    },
    send() {
        
    }
}